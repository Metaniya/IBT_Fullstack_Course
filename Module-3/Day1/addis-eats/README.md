# Addis Eats — React

A static Addis Eats menu built with React and Vite. Shows a Header
component and a menu of dishes rendered from an array using `map`,
each built from a reusable `Dish` component.

## How to Run

npm install
npm run dev

Then open the local URL shown in the terminal.